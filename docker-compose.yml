version: '3.8'

services:

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    networks:
      - my-network

  frontend:
    build: .
    ports:
      - "9092:80"
    depends_on:
      - backend
    networks:
      - my-network

networks:
  my-network: